<%= form_with model:[book, book_comment] do |f| %>
  <div class="form-group">
    <%= f.label :comment, 'コメント' %>
    <%= f.text_area :comment, rows:'5', id: 'comment_text', class:'form-control book_comment' %>
  </div>
  <div class="form-group">
    <%= f.submit '送信' %>
  </div>
<% end %>